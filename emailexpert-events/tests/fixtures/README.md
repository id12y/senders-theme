# Test fixtures

Captured real HeySummit API responses (redacted) belong here per SPEC
10.1. None exist yet: live discovery is pending (see docs/api-notes.md).
When `wp eex discover` runs against the live API, save representative
list and detail responses for each resource here with emails and any
personal data redacted, and point the mapper tests (M2) at them.
